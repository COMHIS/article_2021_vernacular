# Language analysis

 * Catalog: `r catalog`
 * Entries: `r length(dat[[field]])`
 * Unique entries: `r length(unique(dat[[field]]))`  


## Top languages

```{r top_lang, echo=FALSE, message=FALSE, warning=FALSE}
#Top 5 languages per “national” context, histogram
#The Netherlands based on HPB (and STCN?)
#Austria based on HPB
#Estonia based on HPB

library(ggplot2)
theme_set(theme_bw(20))
catalog <- "hpbd"
field <- "language"
ntop <- 5
p <- top_plot(dat[[field]]$language_primary, ntop = ntop) +
       labs(title = paste(catalog, field)) +
       theme_comhis("discrete")
print(p)
```

